
<ul>
  <% @tasks.each do |task| %>
     <li><%= link_to task.title, task_path(task.id) %></li>
     <%= link_to "delete", task_path(task.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
  <% end %>
</ul>
<%= link_to 'Ad a new task', new_task_path %>
